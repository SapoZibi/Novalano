[返回目錄](/Doce/doce.md)

┌──────────────────────────────────────────────────────────┐
│ ⽤⼾端 │
│ 瀏覽器 / S3Drive App（WebDAV） │
└───────────────┬─────────────────────────────┬────────────┘
│ ⾴⾯訪問 / API 請求 │ 上傳 / 下載（直連）
▼ ▼
┌──────────────────────┐ ┌────────────────────────┐
│ 任意前端節點 │ │ 最近能⼒節點 │
│ （靜態 HTML/JS） │ │ （中轉 + 可選存儲） │
│ 所有能⼒節點均可跑 │ │ 流量直達，不過前端 │
└──────────┬───────────┘ └────────────┬───────────┘
│ API │ ⽂件推送
▼ ▼
┌──────────────────────────────────────────────────────────┐
│ 能⼒節點集群（Raft） │
│ │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐ │
│ │ 能⼒節點A │ │ 能⼒節點B │ │ 能⼒節點C │ │
│ │ Leader ★ │ │ Follower │ │ Follower │ │
│ │ 中轉+存儲 │ │ 中轉+緩存 │ │ 中轉+存儲 │ │
│ │ 前端 ✓ │ │ 前端 ✓ │ │ 前端 ✓ │ │
│ └────────────┘ └────────────┘ └────────────┘ │
│ │
│ PostgreSQL 主從複製（實時，毫秒級延遲） │
│ 前端靜態⽂件同步（版本更新時廣播） │
└──────────────────────┬───────────────────────────────────┘
│ 定時備份
▼
┌──────────────────────────────────────────────────────────┐
│ 備份節點（可選） │
│ 增量⽂件備份 + PostgreSQL dump │
│ 不對外提供服務，不參與 Raft │
└──────────────────────────────────────────────────────────┘
